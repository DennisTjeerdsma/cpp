// double slope = static_cast<double>(j/i);

// the static_cast<double>converts the result of the expression j/i to a double type, j/i is int by truncation and then converted to a double and assigned to slope
