// (a) errors, the function returns a string while in the function name is is declared that the return should be int

// (b) needs a return type

// (c) parameters cannot have the same variable name

// (d) body needs braces

