// Parameters are declared in the function parameter list, these are local valuables that are initialized by the arguments

// arguments are supplied to the function call and initialize the parameters.

